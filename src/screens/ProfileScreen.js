import React from 'react';
import { View } from 'react-native';

/**
 * ProfileScreen
 * Экран профиля очищен. Фон полностью черный (#000000).
 * @returns {JSX.Element}
 */
const ProfileScreen = () => {
  return (
    <View style={{ flex: 1, backgroundColor: '#000000' }} />
  );
};

export default ProfileScreen; 